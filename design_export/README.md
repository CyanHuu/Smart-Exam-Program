# 智能排考系统设计稿中转包

这是根据现有 `main_gui.py` 的界面结构还原的 HTML 设计稿，适合作为 MasterGo 的 HTML 转设计稿输入。

使用方式：

1. 在浏览器打开 `index.html` 检查页面。
2. 在 MasterGo 中使用“HTML 转设计稿”导入 `index.html`。
3. 导出 MasterGo/Figma 设计稿后，再上传到网易 CodeWave。

当前包含：排考工作台、考试场次、排考结果表、运行日志、教师管理和数据导出入口。
